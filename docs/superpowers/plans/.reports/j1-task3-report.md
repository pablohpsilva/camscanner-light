# J1 Task 3 Report

**Status:** DONE

**Commit:** `5a27e27` — `test(j1): BDD + on-device export-all-images tests, verify script, index`

**Summary:** Created BDD feature, 2 new step defs, generated integration test, on-device Drift test, verify script, and plans-index row for J1.

---

## Generated test imports (verification)

`apps/mobile/integration_test/j1_export_all_images_test.dart` (generated by build_runner):

New steps:
```
import './../test/step/i_export_all_pages_as_images.dart';
import './../test/step/i_see_the_all_images_export_confirmation.dart';
```

Reused steps:
```
import './../test/step/the_app_is_launched_with_camera_permission_granted_and_empty_storage.dart';
import './../test/step/i_tap_the_scan_button.dart';
import './../test/step/i_capture_and_accept_the_first_page.dart';
import './../test/step/i_tap_done.dart';
import './../test/step/i_open_the_first_document.dart';
```

Generated function names match step def function names exactly:
- `iExportAllPagesAsImages` ← `i_export_all_pages_as_images.dart`
- `iSeeTheAllImagesExportConfirmation` ← `i_see_the_all_images_export_confirmation.dart`

---

## Analyze result

`flutter analyze --no-fatal-infos` → `No issues found! (ran in 2.9s)`

No unrelated generated files were modified by build_runner.

---

## Concerns for parent to verify on-device

1. **BDD scenario** (`integration_test/j1_export_all_images_test.dart`) — requires real camera + capture flow on device RZCY51D0T1K. The scenario opens the menu and taps `page-viewer-export-all-images`, then checks `find.textContaining('Exported')`. The snackbar text is `'Exported N images'` (or `'Exported 1 image'`).

2. **Device test** (`integration_test/j1_export_all_images_device_test.dart`) — deterministic Drift test with no camera; should pass on any device.

3. **Verify script** `scripts/verify/j1.sh` — run with `VERIFY_SKIP_DEVICE=1` for host-only gate, or without for full on-device gate.
