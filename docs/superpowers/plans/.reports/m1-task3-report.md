# M1 Task 3 Report

**Status:** COMPLETE

**Commit hash:** (see below — committed after this report)

**One-line summary:** BDD feature, two new step defs, generated test, deterministic on-device split test, verify script, and plans-index row for M1.

## Generated test imports (verified)

`integration_test/m1_split_document_test.dart` imports:
- New steps: `i_split_after_the_first_page.dart`, `i_see_the_split_confirmation.dart`
- Reused steps: `the_app_is_launched_with_camera_permission_granted_and_empty_storage.dart`, `i_tap_the_scan_button.dart`, `i_capture_and_accept_the_first_page.dart`, `i_capture_and_accept_the_second_page.dart`, `i_tap_done.dart`, `i_open_the_first_document.dart`

## Flutter analyze
`No issues found!` — generated test, device test, and step defs all compile cleanly.

## Files created/modified
- `apps/mobile/integration_test/m1_split_document.feature` (created)
- `apps/mobile/test/step/i_split_after_the_first_page.dart` (created)
- `apps/mobile/test/step/i_see_the_split_confirmation.dart` (created)
- `apps/mobile/integration_test/m1_split_document_test.dart` (generated by build_runner)
- `apps/mobile/integration_test/m1_split_document_device_test.dart` (created)
- `scripts/verify/m1.sh` (created, chmod +x)
- `docs/superpowers/plans/00-plans-index.md` (M1 row added)

## Concerns for parent to verify on-device
1. BDD scenario (`m1_split_document_test.dart`) — the camera stays open after first page accept; second capture+accept appends to the same document. On-device, the camera mock must produce two distinct fake images for both accepts. Check that `iCaptureAndAcceptTheSecondPage` works in sequence without the review screen blocking the second tap.
2. Device test (`m1_split_document_device_test.dart`) — uses `NativeDatabase.memory()` so it runs headlessly; verify it passes on Samsung RZCY51D0T1K.
3. `scripts/verify/m1.sh` — run `VERIFY_SKIP_DEVICE=0 bash scripts/verify/m1.sh` from repo root once device is attached.
