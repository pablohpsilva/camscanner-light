# SDD Progress Ledger — CamScanner-light

Branch: `feat/step-0-monorepo-foundation`

Tasks marked complete here are DONE — do not re-dispatch. Resume at the first
task not marked complete. Trust this ledger + `git log` over recollection.

## Steps

- Step 0 (Monorepo foundation): **complete & harness-verified**. `scripts/verify/step-0.sh` → GATE: PASS (12/12), confirmed by an independent adversarial verifier: literal `nx run mobile:run` launched on BOTH Android emulator and iOS simulator (negative controls + Dart VM Service markers + screenshots), analyze/test green (cache disabled), tree clean, nx-flutter patch applied. Deviations: nx pinned 20.8.4, `@nxrocks/nx-flutter@10.0.1` patched (Node 24 fix), `:project` generator, pnpm-11 build settings.
- **A1 (app shell: Documents home + Scan button): complete & harness-verified.** Commits 6a4a066 (feat) + 2a31bf6 (verify script) + 0581b6b (device-signal hardening). `scripts/verify/a1.sh` → GATE: PASS 9/9; task reviewer APPROVED; independent verifier agreed; screenshots show the real Documents home (AppBar "Documents", "No documents yet", Scan FAB) on Android + iOS. Harness hardened: Android=device resumed-state, iOS=fresh-log VM-service marker, screenshots settle past splash (see VERIFICATION.md).
- **On-device integration-test harness added (commit aeda948):** feature steps now assert the rendered widget tree on the real device via `integration_test` (run by `verify_integration_{android,ios}` in lib.sh, with infra-retry that never retries assertion failures). A1's `a1_home_screen_test.dart` is the first; **independently verified** — A1 GATE PASS 9/9, Step 0 12/12, and an independent mutation check confirmed the test FAILS on a false assertion (non-vacuous). Screenshots are now corroborating-only. Every UI-bearing step ships an integration test (see VERIFICATION.md).
- **A2 (camera preview + permission): IN PROGRESS.** Plan: `docs/superpowers/plans/2026-06-27-a2-camera-preview.md` (commit e0d59cd). Executing task-by-task under subagent-driven-development.
  - Task 1 (plugins + native permission config): complete (commit 66295b1, spec ✅ + quality Approved). camera ^0.12.0+1, permission_handler ^12.0.3; Android CAMERA permission + iOS NSCameraUsageDescription added.
  - Task 2 (state machine: interfaces + ScanController + fakes + unit tests): complete (commits 1779d5b + fix 4beb985, spec ✅ + quality Approved after fix). 6/6 unit tests; narrowed lint ignore to targeted lines; exact notify-count assertion. NOTE: fake_scan.dart holds ONLY the two fake classes; the three *ScanDependencies() preset helpers + the scan_dependencies import are deferred to Task 3.
  - Task 3 (plugin impls + ScanDependencies root + appended fake helpers): complete (commit b1016ef, spec ✅ + quality Approved). 10/10 suite; no plugin API adaptations needed (camera 0.12.0+1, permission_handler 12.0.3 match plan). const composition root verified const-constructible; impl imports isolated to scan_dependencies.dart. Minor (final-review notes): redundant explicit `PluginCameraPreviewController()` ctor; defensive buildPreview null-check; closure-vs-tearoff style in unavailableScanDependencies.
  - Task 4 (CameraScreen + 3 per-state views + widget tests): complete (commit 9772b90, spec ✅ + quality Approved). 13/13 suite; exact strings/keys verified (AppBar 'Scan', rationale, 'Open Settings' FilledButton, 'Camera unavailable on this device', Key('scan-preview')/('scan-checking')); exhaustive switch, no _impl leak; lifecycle correct. Minor (final-review notes): redundant `() => onOpenSettings()` closure; CameraScreen "tap delegates" test is live-fire only (delegation itself is unit-tested in T2); permanentlyDenied view-branch untested (flag is unit-tested in T2).
  - Task 5 (Scan FAB → camera nav + injectable deps through entrypoint): complete (commits be3a0d5 + fix 281f569, spec ✅ + quality Approved after fix). 14/14 suite clean; dependency chain runCamScannerApp→CamScannerApp→HomeScreen→CameraScreen verified; fixed the legacy "tappable Scan button" test to assert the FAB handler without triggering a real-plugin navigation (removed latent flake).
  - Task 6 (on-device integration tests + a2.sh gate): complete (commits 9106fe5 + fixes f73f4e6, b81da16, spec ✅ + quality Approved after fixes). Added a2_camera_denied_test.dart + a2_camera_ready_test.dart (inject fakes via runCamScannerApp; assert camera screen widget tree on device); scripts/verify/a2.sh (15 checks incl. 4 device integration runs). Review caught a missing `find.text('FAKE PREVIEW')` assertion (added). Hardened lib.sh: per-(platform,test) integration evidence logs (no overwrite). Whole-feature opus review caught an Important use-after-dispose race (dismiss mid-`start()`) → fixed ScanController to be dispose-safe (b81da16; race test confirmed to fail against un-fixed controller).
- **A2 (camera preview + permission): COMPLETE & HARNESS-VERIFIED.** Final commit b81da16. `bash scripts/verify/a2.sh` → exit 0, GATE: PASS (15/15) incl. 4 on-device integration runs (denied+ready × Android emulator + iOS simulator), each with a distinct evidence log showing "All tests passed!". Independently verified on f73f4e6 (GATE PASS + both integration tests mutation-checked non-vacuous + VERIFY_SKIP_DEVICE=1 → FAIL); the only post-verifier delta is the dispose-safety fix (logic-only, unit-tested with a race test, device path unchanged, full gate re-run green on b81da16). Known non-blocking limitations logged in VERIFICATION.md (#4: verify_integration retry false-FAILs on transient `[E]` infra errors — fail-safe) and as Minor notes (dead `permanentlyDenied` view param; iOS `restricted`→permanentlyDenied; no permission re-check on resume; scattered const ScanDependencies() defaults).
- **A2 TEST-HARDENING (user-requested improvements): COMPLETE.** Plan `docs/superpowers/plans/2026-06-27-a2-test-hardening-bdd.md`. `bash scripts/verify/a2.sh` → GATE: PASS 14/14 (commit d6d83bc).
  - Coverage floor added to gate (`assert_coverage_floor`, 77% ≥ floor 70%) — commit f5b29cd.
  - Executable Gherkin BDD adopted: `bdd_widget_test` + `.feature` → generated on-device tests; A2's 3 scenarios (denied/granted/unavailable) authored in `integration_test/a2_scan_permission.feature`, step defs in `test/step/`, generated test committed; runs on BOTH Android + iOS. Hand-written denied/ready/unavailable device tests removed (DRY). Standard documented for A3+ (commit 87fe64f; VERIFICATION.md "BDD scenario authoring standard").
  - **ROOT-CAUSE FIX (major):** the recurring "device offline / Failed to start Dart Development Service" Android instability was NOT host/emulator hardware — it was **orphaned `dart development-service` daemons leaking from killed `flutter test` runs** (16 had piled up). `_kill_stale_dds` now runs before each device test; retry also discriminates infra vs real `TestFailure` and recovers an offline emulator. Gate now runs clean (commit d6d83bc).
  - **Real-plugin RUNTIME test NOT gateable** — proven: real `permission_handler.request()` raises the OS permission dialog (`GrantPermissionsActivity`) which `integration_test` can't tap, and `flutter test` installs→runs→**uninstalls** (no pre-grant window); Android camera2 also needs the OS CAMERA grant. So a real preview can't render under `flutter test`. Resolution ("Both"): the BDD gate exercises real plugins by compile+link only; the real camera/permission is a **documented manual check** `apps/mobile/tool/manual_real_camera_check.sh` (`adb install -g` bypasses the dialog — verified: rendered the emulator's real virtual-scene camera, logcat `state=OPEN` + "First frame done"). iOS simulator has NO camera (Apple limitation) → "Camera unavailable" is correct there; real iOS camera = physical device only. Documented VERIFICATION.md #5.
  - **Real-device smoke lane: planned for A3** (opt-in `REAL_DEVICE=1`, separate from the fast gate; Android pre-grant via adb for a real-camera gate, iOS real camera = manual Allow-once). Payoff begins at capture (A3). See VERIFICATION.md #5.
- **A3 (capture photo → review screen): COMPLETE & HARNESS-VERIFIED.** `bash scripts/verify/a3.sh` → exit 0, GATE: PASS (15/15) incl. 2 on-device BDD integration runs (3 scenarios × Android emulator + iOS simulator), each showing "All tests passed!". Mutation check confirmed non-vacuous (false key assertion → TestFailure → revert → green). Negative control VERIFY_SKIP_DEVICE=1 → GATE: FAIL (exit 1). Known deferred gap: EXIF/metadata scrubbing applies when files are persisted/exported — designed in Feature 07, tracked for B1 (capture is temp-only in A3).
- **Next: B1 (save photo + document record / storage).**

## A3 execution (plan: docs/superpowers/plans/2026-06-27-a3-capture-review.md, base f7cb03a)
- Task 1: complete (commit ba6a392, review clean — Spec ✅, Approved). 10/10 tests, pristine.
  - Minor (carry to final review): kFakeJpegBytes is a mutable module-level Uint8List (brief-mandated form); happy-path capture test lacks temp-dir addTearDown; PluginCameraPreviewController.capture() guard has no direct unit test (plugin seam — integration-only, by design).
- Task 2: complete (commit 0043aa4, review clean — Spec ✅, Approved). 5/5 new, scan suite 19/19. Concurrency design verified race-safe (no await between guard-check and _capturing set) + dispose-safe.
  - Minor (carry to final review): ScanController.capture() catch is `on CameraUnavailableException` only — other exception types from preview.capture() would escape (vs the binding "no crash"). No present crash path: production impl maps all CameraException→CameraUnavailableException; both real implementers throw only that. Consider a catch-all→null hardening at final review.
- PLAN FIX (commit 820335e): host widget tests can't route a REAL file through Image.file — pending dart:io isolate-port read hangs flutter_test fake-async (~10min). Tasks 3-4 review tests now use a NON-LOADABLE path; fake gained opt-in captureReturnPath. Real rendering verified on-device (BDD). Empirically proven (variants H/I/J/K/L).
- Task 3: complete (commit 9cdcaf2, review clean — Spec ✅, Approved). 1/1 widget test <1s, no hang. Screen verbatim to brief; non-vacuous (builds image area + both buttons + callbacks fire on tap).
  - Minor (NOT actioned — reviewer assumption empirically false): reviewer suggested asserting review-image-error on the missing-file path "since it resolves synchronously"; my variant-J experiment proved the errorBuilder does NOT render under fake-async pump() for a missing file. errorBuilder branch is a known host-test coverage gap (correct by inspection; would need runAsync to assert). Revisit only if coverage floor fails at Task 5.
- Task 4: complete (commit 04f5a1d, review clean — Spec ✅, Approved). 7/7 widget tests (4 capture + 3 existing) + flutter analyze clean. _onShutter lint-safe (navigator/messenger captured pre-await + mounted guard), heroTag present, fake default branch intact.
  - ⚠️ RESOLVED (by design): Accept→home not asserted at widget level — intentionally covered by the on-device BDD in Task 5 (DRY: avoid duplicating nav-to-home in two layers).
  - Minor (carry to final review): (a) scan-shutter-busy / capturing==true visual branch not widget-asserted (the toggle is unit-tested in Task 2); (b) onShutter typed VoidCallback discards _onShutter's Future — harmless now (capture() returns null, never throws) and fully moot if Task 2's narrow catch is hardened to catch-all. Both robustness-hygiene, same theme as the Task 2 Minor.
- Task 5: complete (BDD scenarios + A3 verify gate + real-device lane). `.feature` authored (3 scenarios), 5 new step defs, generated `a3_capture_review_test.dart` committed, build_runner stub-check clean (no unexpected files), on-device runs PASS (Android + iOS), mutation check non-vacuous, `scripts/verify/a3.sh` → GATE: PASS 15/15. Negative control: GATE: FAIL (exit 1). Coverage 79.2% ≥ 70% floor.
